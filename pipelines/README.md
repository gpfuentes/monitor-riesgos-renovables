# pipelines/
Código que **corre dentro de Databricks**: el recorrido de los datos por la arquitectura medallion.
Se llama `pipelines/` y no `databricks/` para no chocar con la librería `databricks-sdk`.
